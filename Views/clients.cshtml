<h1>Current Clients</h1>

@if (Model.Count != 0)
{
  foreach(var stylist in Model)
  {
    <h3>Name: @stylist.GetFirstName() @stylist.GetLastName()</h3>
    <h4>Expertise: @stylist.GetExpertise()</h4>
  }
}
